/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { interactInstall } from "../../funcs/interactInstall.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  instanceId: z.string(),
  packageInstallRequest: components.PackageInstallRequest$inboundSchema,
};

export const tool$interactInstall: ToolDefinition<typeof args> = {
  name: "interact-install",
  description: `Install Applications

Install Chocolatey packages on a Windows instance. Find a list of packages here: https://community.chocolatey.org/packages.

Requires API key authentication.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await interactInstall(
      client,
      args.instanceId,
      args.packageInstallRequest,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
