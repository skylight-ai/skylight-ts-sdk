/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { windowsPause } from "../../funcs/windowsPause.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PauseRequest$inboundSchema,
};

export const tool$windowsPause: ToolDefinition<typeof args> = {
  name: "windows-pause",
  description: `Pause Vm

Requires API key authentication.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await windowsPause(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
